name: Arduino Compile to UF2

on: [push]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v1

      - name: Install Python Dependencies
        run: |
          pip3 install pyserial

      - name: Install nRF52 Board Core
        run: |
          arduino-cli core update-index --additional-urls https://adafruit.github.io/arduino-board-index/package_adafruit_index.json
          arduino-cli core install adafruit:nrf52@1.7.0 --additional-urls https://adafruit.github.io/arduino-board-index/package_adafruit_index.json

      - name: Compile Code (Copy Headers Directly)
        run: |
          REAL_FQBN="adafruit:nrf52:itsybitsy52840"
          
          # Pfad zum S340-Ordner im installierten Core definieren
          SOFTDEVICE_INC="$HOME/.arduino15/packages/adafruit/hardware/nrf52/1.7.0/cores/nRF5/nordic/softdevice/s340_nrf52_6.1.1_API/include"
          
          # Wir kopieren alle ANT-Header-Dateien direkt in den aktuellen Ordner zu deiner ANT2.ino
          echo "Kopiere ANT+ Header in das Arbeitsverzeichnis..."
          cp "$SOFTDEVICE_INC"/*.h ./
          
          echo "Kompiliere für: $REAL_FQBN"
          
          # Der Compiler läuft jetzt ganz normal ohne riskante Pfad-Flags
          arduino-cli compile --fqbn "$REAL_FQBN" --output-dir ./build ./ANT2.ino

      - name: Upload UF2 Firmware
        uses: actions/upload-artifact@v4
        with:
          name: blipbox-v4-firmware
          path: ./build/
